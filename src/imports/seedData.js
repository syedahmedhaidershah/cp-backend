/** Local dependencies / Libraries */

/** Third party dependencies / Libraries */
const {
    Types: {
        ObjectId
    }
} = require('mongoose');

const seedData = {
}

module.exports = {
    ...seedData
};
